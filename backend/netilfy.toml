[build]
  functions = "netlify/functions" 
  command = "yarn install && yarn build"

[functions]
  node_bundler = "esbuild"
  included_files = ["backend/netlify/functions/firebaseServiceAccountKey.json"]

[dev]
  functions = "netlify/functions"
  port = 5173
